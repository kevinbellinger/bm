<% if @comment.valid? %>
  $('.js-comments').prepend("<%= escape_javascript(render(@comment)) %>");
  $('.new_comment').html("<%= escape_javascript(render partial: 'comments/form', locals: { bookmark: @bookmark, comment: @new_comment }) %>");
  $('.js-comments-count').html("<%= @bookmark.comments.count %> Comments");
<% else %>
  $('.new_comment').html("<%= escape_javascript(render partial: 'comments/form', locals: { bookmark: @bookmark, comment: @comment }) %>");
<% end %>